# E2E Tests

End-to-end tests that verify dewy's deployment lifecycle works correctly against real registries and external services. These tests are executed by [Probe](https://github.com/linyows/probe) via GitHub Actions.

## What These Tests Guarantee

The E2E tests ensure that dewy can:

1. **Detect and deploy an initial version** from a registry, creating the correct symlink structure
2. **Detect a new version** published to the registry and perform a rolling update
3. **Notify** deployment events to Slack (server command with Slack notifier)
4. **Manage containers** including health checks, replica scaling, and non-root execution
5. **Handle multiple port mappings** with TCP proxy for container deployments

All of the above are verified across every supported combination of commands and registries:

| Command | Registry | Description |
|---|---|---|
| `server` | GitHub Releases (`ghr`) | Process management with symlink-based deployment |
| `server` | AWS S3 (`s3`) | Same as above, sourced from S3 |
| `server` | Google Cloud Storage (`gs`) | Same as above, sourced from GCS |
| `assets` | GitHub Releases (`ghr`) | Static asset download and symlink management |
| `assets` | AWS S3 (`s3`) | Same as above, sourced from S3 |
| `assets` | Google Cloud Storage (`gs`) | Same as above, sourced from GCS |
| `container` | OCI / ghcr.io (`img`) | Container deployment with 3 replicas |
| `container` | Docker Hub (`img`) | Same as above, sourced from Docker Hub |
| `container` (multi-port) | OCI / ghcr.io (`img`) | Container deployment with 2 replicas and 2 port mappings |

### Verification Details

**Server / Assets commands:**
- No errors in log output
- Two symlinks created (initial + updated version)
- Two versions started/downloaded (initial + new)
- New version string appears in log

**Container command:**
- No errors in log output (excluding transient `context deadline exceeded`)
- Correct number of containers created for both initial and new versions
- Containers run as non-root user

**Container multi-port command:**
- All of the above container checks
- TCP proxy started on each mapped port
- Backends registered for each port with correct replica count
- HTTP health check returns 200 on both ports

## Architecture

The following diagram is generated by `probe dag --mermaid e2e/test.yml`:

```mermaid
flowchart LR
    subgraph check["Check credentials"]
        check_step0["GITHUB_TOKEN is not empty"]
        check_step1["AWS_ACCESS_KEY_ID is not empty"]
        check_step2["AWS_SECRET_ACCESS_KEY is not empty"]
        check_step3["GOOGLE_APPLICATION_CREDENTIALS is not empty"]
        check_step4["SLACK_TOKEN is not empty"]
    end
    subgraph generate_version["Generate version"]
        generate_version_step0["Generate version string"]
    end
    subgraph build["Build dewy"]
        build_step0["Go build for {{ vars.command }}-{{ vars.registry }}"]
    end
    subgraph start_srv_ghr["Start server by Github-Releases registry with Slack notification"]
        start_srv_ghr_step0["Start dewy"]
        start_srv_ghr_step1["Wait for initial deployment"]
    end
    subgraph start_srv_s3["Start server by AWS S3 registry with Slack notification"]
        start_srv_s3_step0["Start dewy"]
        start_srv_s3_step1["Wait for initial deployment"]
    end
    subgraph start_srv_gs["Start server by GCloud Storage registry with Slack notification"]
        start_srv_gs_step0["Start dewy"]
        start_srv_gs_step1["Wait for initial deployment"]
    end
    subgraph start_ast_ghr["Start assets by Github-Releases registry"]
        start_ast_ghr_step0["Start dewy"]
        start_ast_ghr_step1["Wait for initial deployment"]
    end
    subgraph start_ast_s3["Start assets by AWS S3 registry"]
        start_ast_s3_step0["Start dewy"]
        start_ast_s3_step1["Wait for initial deployment"]
    end
    subgraph start_ast_gs["Start assets by GCloud Storage registry"]
        start_ast_gs_step0["Start dewy"]
        start_ast_gs_step1["Wait for initial deployment"]
    end
    subgraph start_ctr_oci["Start container by OCI registry"]
        start_ctr_oci_step0["Start dewy"]
        start_ctr_oci_step1["Wait for initial deployment"]
    end
    subgraph start_ctr_dockerhub["Start container by Docker Hub registry"]
        start_ctr_dockerhub_step0["Start dewy"]
        start_ctr_dockerhub_step1["Wait for initial deployment"]
    end
    subgraph start_ctr_mp_oci["Start container with multiple ports by OCI registry"]
        start_ctr_mp_oci_step0["Start dewy"]
        start_ctr_mp_oci_step1["Wait for initial deployment"]
    end
    subgraph create_release["Create new version"]
        create_release_step0["Create release"]
        create_release_step1["Verify release exists"]
    end
    subgraph job_13["Verify server by Github-Releases registry"]
        job_13_step0["Server verify"]
        job_13_step1["Wait for new version to start"]
        job_13_step2["Wait for stabilization"]
        job_13_step3["Stop dewy"]
        job_13_step4["Verify no error"]
        job_13_step5["Verify two symlinks creates"]
        job_13_step6["Verify starting two version"]
        job_13_step7["Verify starting new version"]
        job_13_step8["Show log"]
    end
    subgraph job_14["Verify server by AWS S3 registry"]
        job_14_step0["Server verify"]
        job_14_step1["Wait for new version to start"]
        job_14_step2["Wait for stabilization"]
        job_14_step3["Stop dewy"]
        job_14_step4["Verify no error"]
        job_14_step5["Verify two symlinks creates"]
        job_14_step6["Verify starting two version"]
        job_14_step7["Verify starting new version"]
        job_14_step8["Show log"]
    end
    subgraph job_15["Verify server by GCloud Storage registry"]
        job_15_step0["Server verify"]
        job_15_step1["Wait for new version to start"]
        job_15_step2["Wait for stabilization"]
        job_15_step3["Stop dewy"]
        job_15_step4["Verify no error"]
        job_15_step5["Verify two symlinks creates"]
        job_15_step6["Verify starting two version"]
        job_15_step7["Verify starting new version"]
        job_15_step8["Show log"]
    end
    subgraph job_16["Verify assets by Github-Releases registry"]
        job_16_step0["Assets verify"]
        job_16_step1["Wait for new version download"]
        job_16_step2["Wait for stabilization"]
        job_16_step3["Stop dewy"]
        job_16_step4["Verify no error"]
        job_16_step5["Verify two symlinks creates"]
        job_16_step6["Verify starting two version"]
        job_16_step7["Verify starting new version"]
        job_16_step8["Show log"]
    end
    subgraph job_17["Verify assets by AWS S3 registry"]
        job_17_step0["Assets verify"]
        job_17_step1["Wait for new version download"]
        job_17_step2["Wait for stabilization"]
        job_17_step3["Stop dewy"]
        job_17_step4["Verify no error"]
        job_17_step5["Verify two symlinks creates"]
        job_17_step6["Verify starting two version"]
        job_17_step7["Verify starting new version"]
        job_17_step8["Show log"]
    end
    subgraph job_18["Verify assets by GCloud Storage registry"]
        job_18_step0["Assets verify"]
        job_18_step1["Wait for new version download"]
        job_18_step2["Wait for stabilization"]
        job_18_step3["Stop dewy"]
        job_18_step4["Verify no error"]
        job_18_step5["Verify two symlinks creates"]
        job_18_step6["Verify starting two version"]
        job_18_step7["Verify starting new version"]
        job_18_step8["Show log"]
    end
    subgraph job_19["Verify container by OCI registry"]
        job_19_step0["Container verify"]
        job_19_step1["Wait for new version to start"]
        job_19_step2["Wait for stabilization"]
        job_19_step3["Verify containers are not running as root"]
        job_19_step4["Stop dewy"]
        job_19_step5["Verify no error"]
        job_19_step6["Verify current 3 containers creates"]
        job_19_step7["Verify new 3 containers creates"]
        job_19_step8["Show log"]
    end
    subgraph job_20["Verify container by Docker Hub registry"]
        job_20_step0["Container verify"]
        job_20_step1["Wait for new version to start"]
        job_20_step2["Wait for stabilization"]
        job_20_step3["Verify containers are not running as root"]
        job_20_step4["Stop dewy"]
        job_20_step5["Verify no error"]
        job_20_step6["Verify current 3 containers creates"]
        job_20_step7["Verify new 3 containers creates"]
        job_20_step8["Show log"]
    end
    subgraph job_21["Verify container with multiple ports by OCI registry"]
        job_21_step0["Container multi-port verify"]
        job_21_step1["Wait for new version to start"]
        job_21_step2["Wait for stabilization"]
        job_21_step3["Verify TCP proxy started on port1"]
        job_21_step4["Verify TCP proxy started on port2"]
        job_21_step5["Verify all TCP proxies started"]
        job_21_step6["Verify backends added to port1"]
        job_21_step7["Verify backends added to port2"]
        job_21_step8["Test connection to port1"]
        job_21_step9["Test connection to port2"]
        job_21_step10["Verify containers are not running as root"]
        job_21_step11["Stop dewy"]
        job_21_step12["Verify no error"]
        job_21_step13["Verify new containers created"]
        job_21_step14["Show log"]
    end

    check --> generate_version
    generate_version --> build
    build --> start_srv_ghr
    build --> start_srv_s3
    build --> start_srv_gs
    build --> start_ast_ghr
    build --> start_ast_s3
    build --> start_ast_gs
    build --> start_ctr_oci
    build --> start_ctr_dockerhub
    build --> start_ctr_mp_oci
    start_srv_ghr --> create_release
    start_srv_s3 --> create_release
    start_srv_gs --> create_release
    start_ast_ghr --> create_release
    start_ast_s3 --> create_release
    start_ast_gs --> create_release
    start_ctr_oci --> create_release
    start_ctr_dockerhub --> create_release
    start_ctr_mp_oci --> create_release
    create_release --> job_13
    create_release --> job_14
    create_release --> job_15
    create_release --> job_16
    create_release --> job_17
    create_release --> job_18
    create_release --> job_19
    create_release --> job_20
    create_release --> job_21
```

### Phase Details

**Phase 0 (Setup)** validates that all required credentials are available, generates a unique version string using the current Unix timestamp, and builds 9 dewy binaries -- one per command/registry combination.

**Phase 1 (Initial Deployment)** starts all 9 dewy instances in parallel as background processes. Each instance polls its registry, discovers the latest pre-release version of `linyows/dewy-testapp`, and performs an initial deployment. The test waits for log evidence that deployment completed before proceeding.

**Phase 2 (Trigger Update)** creates a new GitHub Release on the `linyows/dewy-testapp` repository using the version generated in Phase 0. This simulates a real version publish that all running dewy instances should detect.

**Phase 3 (Verification)** runs all verification jobs in parallel. Each verify job waits for the new version to appear in the instance's log, then stops the process and inspects the log for expected behavior. Verification logic is defined in `jobs/*-verify.yml`.

## Directory Structure

```
e2e/
├── README.md               # This file
├── test.yml                # Main test definition (Probe format)
├── jobs/                   # Reusable verification job definitions
│   ├── server.yml          # Server start + verify (standalone)
│   ├── server-verify.yml   # Server verification steps
│   ├── assets.yml          # Assets start + verify (standalone)
│   ├── assets-verify.yml   # Assets verification steps
│   ├── container.yml       # Container start + verify (standalone)
│   ├── container-verify.yml           # Container verification steps
│   ├── container-multiport.yml        # Multi-port start + verify (standalone)
│   └── container-multiport-verify.yml # Multi-port verification steps
├── server/{ghr,s3,gs}/     # Working directories for server tests
├── assets/{ghr,s3,gs}/     # Working directories for assets tests
├── container/{img,dockerhub}/         # Working directories for container tests
└── container-multiport/img/           # Working directory for multi-port test
```

## Running

The tests are triggered by GitHub Actions in two ways:

- **Issue comment**: posting `/e2e` on an issue or pull request
- **Workflow dispatch**: manually from the Actions tab
